apiVersion: v1
kind: Pod
metadata:
  name: cuda-vectoradd-no-requests
spec:
  restartPolicy: OnFailure
  containers:
  - name: cuda-vectoradd-no-requests
    image: "quay.io/tardieu/vectoradd:0.1.0"
    resources:
      limits:
        nvidia.com/mig-1g.5gb: 1